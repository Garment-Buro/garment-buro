# Перенос production — 8 сентября 2026

Статус: подготовка нового окружения; публичные домены ещё не переключены.

## Выполнено

- Отдельный TLS-сертификат `production.garment-buro.ru`, проверены HTTPS и Certbot dry-run. На домене пока заглушка 503.
- Сохранена старая SQLite через SQLite backup API, `PRAGMA integrity_check` — `ok`. Сохранены uploads, nginx и compose в `/srv/garment-buro/backups/legacy-production-20260908` (сервер).
- Новое rootless-окружение в `/srv/garment-buro/production`, без подключения к базе dev. Независимые секреты БД, MinIO, JWT и шифрования; повторно использован только транспорт SMTP. Секреты находятся в приватном серверном `.env`.
- Подняты PostgreSQL, Redis и MinIO; выполнены реальные миграции до `20260908_0035`.
- Импортирован каталог: 5 товаров, 21 вариант, 83 медиаобъекта. Неиспользуемые исходные uploads не удалялись.
- Импортированы 5 старых заказов, 11 позиций и 5 платёжных ссылок. У одного заказа историческая сумма отличается от суммы позиций: исходные значения сохранены, подтверждение платежа у провайдера не выполнялось.
- Собраны linux/amd64 образы локально: frontend `garment-buro-frontend:production-cec9b95`, backend `garment-buro-backend:production-terminal-20260908`. Frontend включает identity v2 и CRM UI.
- Исправлена обязательность ключей отключённых провайдеров: `8597bd1`, ветка `features/production-terminal-deploy`. JWT и SMTP остаются обязательными, включённые провайдеры продолжают требовать свои ключи.
- Проверки backend: 434 passed, 3 skipped; Ruff и offline SQL прошли. Три пропуска относятся к отдельным PostgreSQL-тестам конкуренции; успешная миграция на сервере их не заменяет.

## Блокер аккаунтов

В исходной SQLite четыре аккаунта. План identity валиден для трёх; запись ID 3 содержит некорректную почту и не имеет Telegram ID. Строгий импорт всей identity не выполнен. Пользователю задан вопрос: оставить проблемную запись только в сохранённой старой базе или сначала исправить её данные. Никакие аккаунты не удалены и адреса не выдуманы.

## Перед публичным переключением

1. Получить решение по ID 3, сохранить исходник неизменным, подготовить отдельно проверенный источник identity и зафиксировать исключение, если оно одобрено.
2. Выполнить identity-импорт с проверенным fingerprint; включить identity, CRM, приватные файлы, fulfillment CRM и терминал. Назначить согласованному аккаунту роль через `bootstrap_role`, с проверкой ID.
3. Проверить импорт контента каталога и корзин отдельно: они ещё не переносились. Не подставлять фиктивные fingerprints.
4. Включить чтение новых заказов и каталога только после проверки миграционных записей. Не выставлять legacy auth/orders/admin наружу как обход выключенных v2-маршрутов.
5. Не включать платежи, банковские выплаты или создание отправлений СДЭК. На входном nginx явно закрыть неподготовленный checkout и legacy webhook; одних новых feature flags недостаточно из-за legacy fallback.
6. Запустить необходимые воркеры; сверить CRM intake, не превращая старые платёжные ссылки автоматически в подтверждённые оплаты.
7. Сделать post-import backup PostgreSQL и MinIO; проверить восстановление. Текущий штатный `backup-server.sh` ещё не покрывает MinIO, одного его недостаточно.
8. Проверить новый backend на `172.17.0.1:8200`, frontend на `172.17.0.1:3200`, MinIO на `172.17.0.1:9200`. Затем подготовить diff nginx с сохранением dev/widget и ACME. Не заменять общий nginx целиком непроверенным шаблоном.
9. Приёмка: `/` на production-хосте ведёт в `/production`, корректный manifest, авторизация и refresh, права покупателя/сотрудника, QR и файлы. Статус 200 страницы входа не является проверкой рабочего терминала.

## Восстановление контекста

SSH периодически отклоняет/обрывает соединения; проверять результат команд и миграционные записи перед повтором. Каталожный импорт требует пустого target: его нельзя повторять вслепую после успешного применения.

Временные локальные артефакты сборки и передачи: `/tmp/gb-production-release.wS8eYY`. Там нет серверных паролей. На сервере сохранены скрипты `prepare_env.py` и `update_settings.py`; первый предназначен только для первичного создания `.env` и повторно запускаться не должен. Публичная старая система сохранена для отката и продолжает обслуживать домены до явного cutover.
