# Перенос production — 8 сентября 2026

Статус: основной сайт, `www`, кабинет партнёра и производственный терминал переключены на новое production-окружение. Dev и widget оставлены на своих прежних сервисах.

## Выполнено

- Отдельный TLS-сертификат `production.garment-buro.ru`, проверены HTTPS и Certbot dry-run. Заглушка заменена терминалом: `/` → `/production`.
- Сохранена старая SQLite через SQLite backup API, `PRAGMA integrity_check` — `ok`. Сохранены uploads, nginx и compose в `/srv/garment-buro/backups/legacy-production-20260908` (сервер).
- Новое rootless-окружение в `/srv/garment-buro/production`, без подключения к базе dev. Независимые секреты БД, MinIO, JWT и шифрования; повторно использован только транспорт SMTP. Секреты находятся в приватном серверном `.env`.
- Подняты PostgreSQL, Redis и MinIO; выполнены реальные миграции до `20260908_0035`.
- Импортирован каталог: 5 товаров, 21 вариант, 83 медиаобъекта. Неиспользуемые исходные uploads не удалялись.
- Импортированы 5 старых заказов, 11 позиций и 5 платёжных ссылок. У одного заказа историческая сумма отличается от суммы позиций: исходные значения сохранены, подтверждение платежа у провайдера не выполнялось.
- Собраны linux/amd64 образы локально: frontend `garment-buro-frontend:production-cec9b95`, backend `garment-buro-backend:production-terminal-20260908`. Frontend включает identity v2 и CRM UI.
- Исправлена обязательность ключей отключённых провайдеров: `8597bd1`, ветка `features/production-terminal-deploy`. JWT и SMTP остаются обязательными, включённые провайдеры продолжают требовать свои ключи.
- Проверки backend: 434 passed, 3 skipped; Ruff и offline SQL прошли. Три пропуска относятся к отдельным PostgreSQL-тестам конкуренции; успешная миграция на сервере их не заменяет.

## Завершение импорта и доступ

После явного разрешения пользователя удалена только запись ID 3 из рабочей SQLite для миграции. Проверены отсутствие связей с заказами и FK; сохранён SQLite backup `before-delete-user-3.sqlite3` в production legacy volume. Неизменная старая база также остаётся в архиве и старом окружении для отката. Запись восстановима.

- Импортированы три корректных аккаунта. `it.mikita@gmail.com` соответствует целевому ID 1, ему штатным bootstrap-сервисом назначена роль `production_tech`.
- Из прежнего кабинета партнёра восстановлен единственный профиль `it-mikita`: Mikita, active, комиссия 0 bps. Перед переносом подтверждено отсутствие в dev партнёрских лендингов, начислений, выплат и реквизитов. Финансовые записи и ключи dev не копировались.
- Перенесены шесть Redis-корзин, 19 позиций, с сохранением оставшегося TTL, затем импортированы в PostgreSQL и включён `CARTS_V2_ENABLED`. Старая Redis не очищалась.
- Импортированы два документа настроек каталога. Чтение каталога и заказов, identity, партнёрская программа, CRM, приватные файлы, production terminal и fulfillment CRM включены с проверенными fingerprints.
- Backend, frontend, notification-worker и fulfillment-worker запущены. Readiness: PostgreSQL/MinIO ready. CRM reconciliation с проверкой объектов: healthy, 0 проектов/вещей/приватных файлов. Старые платёжные ссылки не превращались в подтверждения оплаты.

## Проверки после переключения

- Все шесть хостов проверены с HTTPS: сайт, www, dev, partner и widget — 200; production — 307 на HTTPS `/production`.
- В Chrome проверена загруженная форма «Вход на участок» с рабочей почтой; это не проверка вошедшего сотрудника или полного производственного цикла.
- Манифест production содержит отдельные `id`, `scope`, `start_url` `/production`, display standalone.
- `/api/production/me` без токена — 401. Refresh с production Origin без cookie — 401, не CORS 403. QR generator — 200 image/png.
- Публичный файл нового MinIO — 200; анонимное обращение к приватному bucket — 403. Прокси приватного bucket сохраняет внутренний Host для SigV4, доступны только GET/HEAD.
- SMTP login на 465 SSL — OK. Доставку письма в почтовый ящик и пользовательский вход по OTP в этой приёмке не подтверждали.
- Checkout POST и старый YooKassa webhook намеренно закрыты на nginx с 503. Банковские выплаты, создание платежей/отправлений и tracking не включались; внешние финансовые операции не выполнялись. Изменение каталога и legacy upload также закрыты на входном прокси до отдельного включения write API.

Первое переключение выявило блокировку внутренних портов UFW; выполнен откат nginx, затем исправление и повторная проверка. Теперь разрешены только TCP 3200/8200/9200 от Docker-сети nginx `172.19.0.0/16` к `172.17.0.1`. Новых публичных разрешений портов нет. Активный nginx: `/home/plus2opacity/nginx.conf`; предыдущий файл: `nginx.conf.before-production-cutover-20260908` рядом с ним.

## Резервные копии и последующие работы

Post-import backup: `/srv/garment-buro/backups/production-post-import-20260908` на сервере. PostgreSQL dump восстановлен в отдельную проверочную БД: 5 заказов, 3 пользователя, 5 товаров. Проверочная БД после проверки удалена. Архив MinIO прошёл `gzip -t`; полноценный запуск MinIO из восстановленного архива отдельно не проверялся.

## Следующие этапы

1. Пользовательский вход по рабочей почте и контрольный заказ по цеху: реальные QR-сканер, принтер, оригиналы, техкарты и переходы участков.
2. Подготовить производственные справочники и задания: сейчас очередь пустая, демонстрационные заказы в production не создавались.
3. Отдельно подключить и проверить платёжные/доставочные интеграции; только после этого включать checkout и снимать соответствующие nginx-запреты.
4. Дополнить регулярный backup MinIO и проверкой восстановления; текущий штатный `backup-server.sh` не покрывает MinIO. Ручной архив не заменяет регулярное резервирование.
5. Старый стек оставлен для отката. Не возвращать старую базу после новых записей в production без сверки данных. Не выполнять разрушительный downgrade миграций.

## Восстановление контекста

SSH периодически отклоняет/обрывает соединения; проверять результат команд и миграционные записи перед повтором. Каталожный импорт требует пустого target: его нельзя повторять вслепую после успешного применения.

Временные локальные артефакты сборки и передачи: `/tmp/gb-production-release.wS8eYY`. Там нет серверных паролей. На сервере сохранены скрипты `prepare_env.py` и `update_settings.py`; первый предназначен только для первичного создания `.env` и повторно запускаться не должен. Старый стек сохранён для отката, но основной сайт и partner теперь обслуживает новый production.
