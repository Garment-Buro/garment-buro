/**
 * Product data remains available to landing selection and the constructor,
 * while the legacy public catalog and standalone product pages are paused.
 */
export const PUBLIC_CATALOG_ENABLED = false;
